#include <stdio.h>

int main(void)
{
    puts("aurora-compositor");
    puts("target: Wayland compositor for Raspberry Pi and PCs");
    puts("backend: wlroots/DRM/KMS design target");
    puts("policy: square server-side decorations, integer shell scaling, pixel UI only");
    puts("next: initialize wlroots backend, output layout, seat, xdg-shell, Aurora shell protocol");
    return 0;
}
